# Nexmem — Current Status Report

> Generated for external review. Reflects only what was verified in the local
> workspace at the SHA below. Items that could not be verified are marked
> **UNVERIFIED**. No secrets are included in this file.

────────────────────────────────────────
## 1. Project Identity
────────────────────────────────────────
- Project name: **Nexmem**
- Date of this report: **2026-05-30** (Block 10 merge-prep, post-rewrite)
- Current git branch: **`chore/merge-prep`**
- Current tip SHA: **`7e0feb8`** (rewritten tip; `origin/docs/sdk-quickstarts` = `7e0feb8` after rewrite). HEAD advances by the 3 Block 10 commits below.
- `origin/main` SHA: **`c2f9212`** (post-rewrite; was `56afcdb` pre-rewrite)
- Remote repo: **`https://github.com/nexmemai/nexmem.git`** (token-free remote — verified)
- Python version: **3.11.9**
- History rewrite: **COMPLETED** — see `HISTORY_REWRITE_COMPLETE.md`. All 22 branches + tags were rewritten with git-filter-repo and force-pushed; no real secrets remain in remote history (see §6 / §7).
- Block 10 commits on this branch (newest last):
  - `fix: restore secret-scanner tripwire after history rewrite` (SHA-256 hash tripwire)
  - `fix: use timezone-aware UTC for token iat/cutoff after rewrite`
  - `docs: Block 10 merge prep — status, runbook, risks, secret prescan`

────────────────────────────────────────
## 2. Tech Stack (from requirements.txt)
────────────────────────────────────────
- **Core API:** fastapi 0.115.0, uvicorn[standard] 0.30.0, pydantic 2.7.1, pydantic-settings 2.3.0, asgiref 3.8.1
- **Database:** sqlalchemy[asyncio] 2.0.30, alembic 1.13.2, asyncpg 0.29.0, psycopg2-binary 2.9.9, pgvector 0.3.0
- **NLP / AI:** spacy 3.7.4, sentence-transformers 3.0.1, openai 1.30.0, numpy 1.26.4, networkx 3.3
- **Auth / Security:** python-jose[cryptography] 3.3.0, passlib[bcrypt] 1.7.4, bcrypt 4.0.1, pyotp 2.9.0, qrcode[pil] 7.4.2, email-validator 2.3.0
- **Background Jobs:** celery 5.4.0, redis 5.0.0, apscheduler >=3.10.0
- **Observability:** sentry-sdk[fastapi] 2.1.1, structlog 24.1.0, prometheus-fastapi-instrumentator 7.0.0, opentelemetry-sdk 1.25.0, opentelemetry-exporter-otlp 1.25.0, opentelemetry-instrumentation-fastapi/sqlalchemy/redis 0.46b0
- **Testing:** (not in requirements.txt — pytest/httpx/respx/locust are dev-only; locustfile present in `tests/`) — UNVERIFIED whether pinned in a separate dev-requirements file
- **Other:** httpx 0.27.0, python-dotenv 1.0.1, slowapi 0.1.9, tenacity 8.3.0

────────────────────────────────────────
## 3. Database Migrations (Alembic)
────────────────────────────────────────
Chain (offline `alembic history`, base → head):
- `001_baseline` — Baseline migration for Supabase schema 001 + 002
- `51d59ebea874` — compatibility no-op for an old autogenerated auth revision
- `002_hnsw_index` — switch to HNSW index for semantic memory
- `003_add_consolidated_at` — add `consolidated_at` to episodic_memory
- `004_add_fts_columns` — FTS tsvector columns + GIN indexes
- `005_add_app_id_procedural` — add `app_id` to procedural_memory
- `006_align_app_scoping` — align app scoping columns + procedural uniqueness
- `007_standardize_vector_dim` — standardize semantic_memory vector to 384D (destructive — R-205)
- `008_enable_memory_rls` — enable RLS for user-scoped memory tables
- `009_engram_hnsw_index` — HNSW index on `engrams.dense_embedding`
- `010_token_usage` — token usage tracking table
- `011_fk_cascade_content_limits` — FK cascade + content length constraints
- `012_refresh_tokens` — refresh_tokens table (real session revocation)
- `013_extend_rls` — extend RLS to api_keys, refresh_tokens, token_usage
- `014_phase3_auth_hardening` — email verification + password reset tokens
- `015_audit_log_tables` — gdpr + auth audit log tables (P10-H1/H2)
- `016_jsonb_shape_checks` — JSONB shape CHECK constraints (P5-C9)
- `017_apps_table` — apps table, multi-tenant app model (P4-B1)
- `018_apikeys_app_fk` — `api_keys.app_id` FK to apps.id (P4-B2)
- `019_app_level_rls` — app-level RLS on the 5 memory tables (P4-B4)
- `020_engrams_app_id` — `engrams.app_id` + app-aware RLS (Amendment 2)
- `021_totp_fields` — TOTP/2FA fields on users (P3-A6, Block 5)
- `022_user_soft_delete` — soft-delete grace period on users (P7-E4, Block 5)
- `023_app_usage_tracking` — per-app usage tracking (P4-B5, Block 7)
- `024_app_suspension` — app suspension columns (P4-B6, Block 7)

**current head = `024_app_suspension`** (offline). `alembic current` against a live DB = UNVERIFIED (no `DATABASE_URL` in this workspace; alembic refuses to run without it).

────────────────────────────────────────
## 4. What Is Confirmed Done
────────────────────────────────────────
- [x] FastAPI app (`app/main.py`) — imports OK
- [x] Config / settings (`app/config.py`) — strict `validate_production`
- [x] Async database (`app/database.py`, 193 lines)
- [x] Health endpoints (`app/routers/health.py`, 122 lines)
- [x] Episodic memory router (`app/routers/episodic.py`, 217 lines)
- [x] Semantic memory router (`app/routers/semantic.py`, 255 lines)
- [x] Procedural memory router (`app/routers/procedural.py`, 140 lines)
- [x] Graph memory router (`app/routers/graph.py`, 374 lines)
- [x] RAG chat router (`app/routers/rag.py`, 297 lines)
- [x] Auth router (`app/routers/auth.py`, 1074 lines — register/login/logout)
- [x] TOTP / 2FA router (`app/routers/totp.py`)
- [x] GDPR router (`app/routers/gdpr.py` — export/delete/cancel-deletion)
- [x] Admin router (`app/routers/admin.py` — force-logout, impersonate, analytics/usage all present)
- [x] Memory unified context endpoint (`app/routers/memory.py`, 565 lines)
- [x] Apps router (`app/routers/apps.py`, 206 lines)
- [x] Engram processor (`app/services/engram_processor.py`)
- [x] Embedding service (`app/services/embedder.py`)
- [x] LLM service (`app/services/llm.py`)
- [x] Consolidation Celery task (`app/services/consolidation.py`, 412 lines)
- [x] Data retention task (`enforce_data_retention` in `app/tasks.py`)
- [x] Scheduled deletion task (`execute_scheduled_deletions` in `app/tasks.py`)
- [x] Queue pressure check (`app/core/queue_pressure.py`)
- [x] Suspension check (`app/core/suspension_check.py`)
- [x] App quota service (`app/services/app_quota.py`)
- [x] Token blocklist (`app/core/token_blocklist.py`)
- [x] Audit log (`app/core/audit_log.py`)
- [x] Circuit breaker (`app/core/circuit_breaker.py`)
- [x] Concurrency module (`app/core/concurrency.py`)
- [x] Admin CLI (`scripts/nexmem_admin.py`, 445 lines)
- [x] Secret scanner (`scripts/scan_secrets.py` — runs clean; SHA-256 hash tripwire)
- [x] Python SDK (`nexmem-py/` — client, sync_client, models, exceptions; wheel+sdist built in `dist/`)
- [x] TypeScript SDK (`nexmem-js/` — client.ts, types, errors, tests)
- [x] MCP server (`nexmem-mcp/` — server.py, tools/, tests/)
- [x] Streamlit dashboard (`frontend/app.py`)
- [x] Docker Compose (`docker-compose.yml`)
- [x] render.yaml (web + celery worker + beat + redis)
- [x] Alembic migrations (001 → 024)
- [x] Test suite (`tests/` — 46 files)
- [x] Incident runbooks (`docs/runbooks/` — POSTGRES, REDIS, OPENAI, BACKUP_RESTORE)
- [x] SLO document (`docs/SLO.md`, 123 lines)
- [x] Data retention doc (`docs/DATA_RETENTION.md`, 114 lines)
- [x] Security policy (`SECURITY.md`)
- [x] Contributing guide (`CONTRIBUTING.md`)
- [x] Bootstrap file (`KIRO_SESSION_BOOTSTRAP.md`)
- [x] Backend risks (`BACKEND_RISKS.md`)
- [x] Examples (`examples/` — python_quickstart.py, javascript_quickstart.mjs, README.md)

All 50 checklist items resolved to **DONE**. None PARTIAL or MISSING in the local tree at this SHA.

────────────────────────────────────────
## 5. Test Suite Summary
────────────────────────────────────────
From `pytest tests/ -q` (demo mode, venv interpreter):
- Total passing: **264**
- Total failing: **0**
- Total skipped: **33**
- Deselected: **5**
- Wall time: ~84s

Gated tests:
- Requiring live DB (`RUN_DB_TESTS=1`): `test_auth.py` (9), `test_memory.py` (12), `test_memory_context.py` (4), `test_transactional_writes_integration.py` (1) + partial in `test_data_retention.py`
- Requiring ML models (`RUN_ML_TESTS=1`): `test_engram_processor.py` (9)

Per-file test counts (def test_*):
```
test_admin_cli 4        test_admin_endpoints 9     test_app_scoping 4
test_app_suspension 6   test_app_usage 7           test_audit_log 12
test_auth 9             test_celery_health_probe 5 test_celery_task_logging 4
test_circuit_breaker 9  test_concurrency 7         test_concurrent_writes 2
test_config_validate 7  test_data_retention 4      test_dlq_admin_cli 8
test_engram_processor 9 test_frontend_auth 4       test_gdpr_hardening 9
test_gdpr_soft_delete 4 test_graceful_shutdown 4   test_isolation_and_write 13
test_json_shape_guard 6 test_llm_service 11        test_lockout_escalation 4
test_logging_redaction 5 test_memory 12            test_memory_context 4
test_migration_lint 9   test_otel_disabled 2       test_p5_p6_p7_hardening 12
test_p6_celery_hardening 14 test_phase3_auth_hardening 16 test_pool_sizing 2
test_queue_pressure 4   test_quickstart_examples 5 test_quotas 11
test_read_only_mode 9   test_secret_scan 5         test_token_blocklist 10
test_token_lifecycle 7  test_totp 6                test_transactional_writes 4
test_transactional_writes_integration 1 test_unit_isolation 3
```
(Plus SDK suites per bootstrap: nexmem-py 8 passed, nexmem-js 6 passed — both UNVERIFIED this session, not re-run.)

────────────────────────────────────────
## 6. What Is NOT Done (Confirmed Missing)
────────────────────────────────────────
- **Stripe / billing integration** — no `stripe` dep, no billing router/module found. MISSING.
- **Per-plan quota enforcement beyond free tier** — quota tiers exist in `app/config.py` (free/starter/pro/enterprise read+write caps) but no plan-assignment / upgrade path. PARTIAL (knobs only).
- **Production deployment** — no live Render URL confirmable from files. `render.yaml` exists; deployment status UNVERIFIED.
- **TOTP 2FA (PR #17)** — PRESENT on this branch (`app/routers/totp.py`, migration 021, 6 tests). Done.
- **App suspension (PR #19)** — PRESENT on this branch (`app/core/suspension_check.py`, migration 024, 6 tests). Done.
- **SDK published to PyPI / npm** — NOT published. nexmem-py wheel built locally only; operator actions #11/#12 pending. MISSING (publish step).
- **CI green on main** — UNVERIFIED (no `gh` CLI; cannot query GitHub Actions from workspace).
- **Git history rewrite (leaked Supabase password + PAT)** — **DONE.** Rewritten across all 22 branches + tags and force-pushed; verified 0 real secrets remain (see §7). Was R-201 / operator action #5.
- **Operator env vars set in Render** — UNVERIFIED (cannot inspect Render from workspace).

────────────────────────────────────────
## 7. Open Risks (from BACKEND_RISKS.md, verbatim — non-RESOLVED only)
────────────────────────────────────────
**HIGH (300-series — must fix before public launch):**
- **R-301** Redis fail-open allows auth / rate-limit bypass during outage — Status: **ACCEPTED** (private beta). Affects brute_force, rate_limit, token_blocklist (fail open); quotas + Celery fail closed.

**MEDIUM / P2–P3 (OPEN or ACCEPTED):**
- **R-302** TOTP session tokens have no dedicated rate limit — **OPEN** (complete-login can be brute-forced).
- **R-303** `execute_scheduled_deletions` requires Celery Beat schedule — **OPEN** (operator action).
- **R-304** `enforce_data_retention` requires Celery Beat schedule — **OPEN** (operator action).
- **R-305** Admin endpoints inert without `ADMIN_API_KEY` — **ACCEPTED**.
- **R-306** `check_app_not_suspended` fails open on DB error — **ACCEPTED**.
- **R-201** Full git-history rewrite required — **RESOLVED** (rewrite completed + force-pushed; see `HISTORY_REWRITE_COMPLETE.md`).
- **R-102** No real session revocation — **ACCEPTED** (4h access tokens; blocklist fails open).
- **R-101** RLS only on memory tables — **MITIGATED** (extended to api_keys, refresh_tokens, token_usage, users).
- **R-106** Blocking NLP/embedding in async routes — **MITIGATED** (per-loop semaphore; global cap still wanted).
- **R-107** NetworkX graph state per-process — **ACCEPTED** (pinned `--workers 1`).
- **R-202** Demo path / prod path duplicate logic — **ACCEPTED**.
- **R-204** No load testing under realistic conditions — **ACCEPTED**.
- **R-205** Migration 007 is destructive — **ACCEPTED** (already ran, fenced).

────────────────────────────────────────
## 7b. Secret Prescan (post-rewrite, this session)
────────────────────────────────────────
Read-only scan of rewritten remote history (`git log --remotes=origin -p`),
classifying raw matches into real secrets vs. regex/doc/placeholder mentions:

| Pattern | Raw matches | Real secrets | Notes |
|---|---|---|---|
| `ghp_` (GitHub PAT) | 9 | **0** | All are the scanner's own `ghp_` detection regex + runbook references to the prefix |
| `sk-` (OpenAI key) | 212 | **0** | 153 placeholders (`sk-placeholder`, `sk-test-`); only key-shaped one is `FAKE_OPENAI_KEY` in `CONTRIBUTING.md` (explicitly fake) |
| Supabase password / project ref | 0 in origin | **0** | Purged by the rewrite |

Working-tree scanner: `python scripts/scan_secrets.py` → **clean**. The scanner
now uses SHA-256 hash tripwires (it no longer stores the cleartext value).

────────────────────────────────────────
## 8. Operator Actions Still Required
────────────────────────────────────────
From `KIRO_WORK_LOG.md` §9 (none confirmed complete):
1. Merge PR #4 (Phase 3+ plan, docs-only)
2. Decide between PR #2 and PR #3 for Phase 2 (keep #3, close #2)
3. Close PR #1 without merge after PR #3 merges
4. Merge canonical stack in order: #3 → #5 → #6 → #7 → #8 → #9 → #10 → #11 → #13 → #14 → Block-3 PR → … → Block 9
5. ~~**Git-history rewrite** to purge leaked Supabase password~~ — **DONE** (completed + force-pushed; see `HISTORY_REWRITE_COMPLETE.md`)
6. **Set Render env vars** (DATABASE_URL, REDIS_URL, SECRET_KEY, OPENAI_API_KEY, SENTRY_DSN, ADMIN_API_KEY, ENVIRONMENT=production; DEMO_MODE unset) (CRITICAL)
7. **Run `alembic upgrade head`** against live DB (target `024_app_suspension`) (CRITICAL)
8. Confirm GitHub Actions CI green on main after each merge
9. Schedule first quarterly backup-restore drill
10. Merge PR #12 (`kiro/work-log`) or formally retire it
11. Publish `nexmem-py` to PyPI (`python -m build && twine upload dist/*`)
12. Publish `nexmem-js` to npm (`npm run build && npm publish --access public`)
13. Configure Celery Beat schedules for `execute_scheduled_deletions` (daily) and `enforce_data_retention` (weekly)

────────────────────────────────────────
## 9. Git Branch State
────────────────────────────────────────
- Current branch is a **feature branch** (`chore/merge-prep`), NOT main. It sits at the rewritten Block 9 tip (`7e0feb8` = `origin/docs/sdk-quickstarts`) plus the 3 Block 10 commits.
- Commits ahead of `origin/main`: **44** before the Block 10 commits (`git rev-list --count origin/main..HEAD`).
- Open PRs: per `KIRO_WORK_LOG.md`, ~21 open PRs (#1–#21 referenced); none merged. UNVERIFIED against live GitHub (no `gh` CLI).
- Has the full hardening stack been merged to main? **NO.** `origin/main` tip = `c2f9212` (post-rewrite). All hardening lives on branches.
- Last commit on main: **`c2f9212`** (`origin/main` / `origin/HEAD`, post-rewrite).
- Note: an `origin/master` also exists (post-rewrite SHA) — divergent from `main`; relationship UNVERIFIED.

────────────────────────────────────────
## 10. Honest Summary
────────────────────────────────────────
The Nexmem backend is feature-complete for a private beta on paper: every router, service, Celery task, migration (001–024), SDK, and doc referenced in the plan exists in the working tree, and the demo-mode test suite is green (264 passed / 0 failed / 33 skipped). Imports succeed, the API key prefix is correctly `nxm_`, and the secret scanner is clean. The leaked Supabase password and GitHub PAT have been **purged from git history** (rewrite completed + force-pushed across all 22 branches; 0 real secrets remain — see §7/§7b). The single biggest remaining gap is that the hardening stack is **not yet on `main`** — `origin/main` (`c2f9212`) does not yet carry the 44-commit stack, so "what runs in production today" is UNVERIFIED and likely not this code. The second biggest gap is that all live-system verification (live DB migrations, Render env vars, CI status, deployed health checks) cannot be confirmed from the workspace and remains operator-owned. Redis fail-open (R-301) is an accepted-but-real auth-bypass risk during an outage. Billing/Stripe is genuinely absent, and the SDKs are built but unpublished. **Recommendation: NO-GO for private beta until the canonical stack is merged to main with CI green, Render env vars are set, and `alembic upgrade head` is confirmed against the live DB.** The history rewrite — previously the top blocker — is now done. Code readiness is strong; release/ops readiness is unproven.

> Security note (updated): the `origin` remote URL is now token-free (verified). Any GitHub PAT previously embedded in the URL or pasted into a session must be treated as exposed and revoked.

────────────────────────────────────────
## 11. Recommended Next Steps
────────────────────────────────────────
1. **Confirm token-free remotes everywhere** and ensure any previously-exposed GitHub PAT is revoked (rotate at github.com/settings/tokens). Use a credential helper, never an inline-token URL.
2. **Merge the canonical stack to `main` in order** (#3 → … → Block 9) with CI green at each step; confirm via GitHub Actions before each subsequent merge. Ensure the scanner-fix commit is present on each branch before expecting CI to pass.
3. **History rewrite is complete** — verify GitHub's secret-scanning alerts have cleared and instruct all collaborators to delete + re-clone (old clones carry pre-rewrite history).
4. **Set Render env vars and run migrations**: configure DATABASE_URL/REDIS_URL/SECRET_KEY/OPENAI_API_KEY/SENTRY_DSN/ADMIN_API_KEY, ensure `DEMO_MODE` unset, then `scripts/run_with_migrations.sh` to reach head `024_app_suspension`; verify `/health/ready` returns 200.
5. **Wire Celery Beat schedules** for `execute_scheduled_deletions` and `enforce_data_retention` (R-303/R-304), and add a dedicated rate limit on `/auth/totp/complete-login` (R-302) before public launch.
